import PokeBall from "../assets/pokeball-logo.png";
import LoadingMagnifyingGlass from "../assets/loading.gif";

const images: Record<string, string | undefined> = {
  pokeball: PokeBall,
  loading: LoadingMagnifyingGlass,
};

export default images;
